# D0.1 — Review Evidence Bundle (Backlog v2 §6)

**Unit:** D0.1 — Foundry design tokens + `/ui` gallery harness · **Lane:** FE ·
**Loop:** 0 · **Builder:** FE (this session) · **Status:** `IN PROGRESS` → submitted
for isolated review. Builder does **not** self-verify (R14); INT marks `VERIFIED`
after REV verdict, then copies the outcome to `PROJECT_STATUS.md`.

## 1. Mini-spec
`docs/specs/D0_DESIGN_SYSTEM.md` (D0.1 section). D0.1's unit DoD in the Instruction Set
§13 is explicitly *"Existing mini-spec; isolated review; no token drift"* — the ½-page
spec is accepted for this unit; the §12.1 11-section template applies to D0.2+.

## 2. SHAs
- **Baseline HEAD at submission:** `5f2a4b5` (BE F0.2 governance persistence).
- **Review SHA:** *not yet cut.* Changes are isolated but currently live in the shared
  `foundry-build` working tree, not an immutable `build/D0.1-tokens-gallery` worktree.
  **INT action needed:** pin a baseline + cut the review SHA per §0B branch protocol.

## 3. Exact file list (D0.1 only — excludes BE F0.2 and the uncommitted v2 spec edits)
| File | Change |
|---|---|
| `frontend/src/ui/tokens.ts` | new — typed token source of truth |
| `frontend/src/ui/tokens.css` | new — Tailwind-4 `@theme` mirror |
| `frontend/src/ui/tokens.test.ts` | new — palette + drift-guard tests |
| `frontend/src/app/ui/page.tsx` | new — `/ui` gallery scaffold |
| `frontend/src/app/ui/ui.test.tsx` | new — gallery render test |
| `frontend/src/app/globals.css` | mod — v3 `@tailwind` → v4 `@import`/`@theme` |
| `frontend/vitest.config.ts` | mod — coverage `include` extended to `src/ui/**`, `src/app/ui/**` |
| `docs/specs/D0_DESIGN_SYSTEM.md` | new — D0 mini-spec |

*Explicitly excluded from this unit's review set:* `docs/specs/BUILD_INSTRUCTION_SET_2026-07.md`,
`docs/specs/DELIVERY_LOOPS_BACKLOG_2026-07.md` (uncommitted INT v2 edits), `.claude/ctx/`.

## 4. Acceptance-criterion → test mapping
| Acceptance criterion (D0.1) | Test / evidence |
|---|---|
| Every Prototype-9 colour token present with exact hex | `tokens.test.ts` › "exposes every Prototype 9 colour token" (`toEqual` on all 16) |
| Font stacks + ordered radius scale exported | `tokens.test.ts` › "display/body/mono font stacks and an ordered radius scale" |
| **No token drift** between `tokens.ts` and `tokens.css` | `tokens.test.ts` › "keeps tokens.css in sync" (reads CSS, asserts every hex present) |
| `/ui` renders a swatch per token | `ui.test.tsx` › "renders the design-tokens section with a swatch per colour token" |
| Tailwind-4 CSS compiles; `/ui` is a real route | `next build` → `/ui` prerendered (○ static) |

## 5. Commands + results (clean environment)
```
npx vitest run            → 5 files, 24 passed (was 20; +4 for D0.1)
npx vitest run --coverage → new code 100% (tokens.ts, page.tsx); overall 96.55%
npx tsc --noEmit          → exit 0
npx eslint                → exit 0
npx next build            → compiled OK; /ui prerendered as static
bash scripts/verify-audit.sh → PASS (unchanged; unit touches no Python)
```

## 6. Coverage / negative / mutation notes
- New files `tokens.ts` + `page.tsx` at **100%** stmts/branch/funcs/lines (v8 JSON summary).
- **Drift-guard is a mutation test in spirit:** editing `tokens.css` away from `tokens.ts`
  (or vice-versa) fails the suite — verified during build (the `fileURLToPath` bug caught
  a real failure before the `process.cwd()` fix).

## 7. Contract / schema / migration diff
None. D0.1 is UI-only; no OpenAPI, no DB, no generated-client impact.

## 8. Threat / data-egress delta
None. Static server component; no network, no auth, no user input, no model calls,
no source text. Serving plane unaffected (R13 untouched).

## 9. Rollback
Delete `frontend/src/ui/`, `frontend/src/app/ui/`, `docs/specs/D0_DESIGN_SYSTEM.md`;
revert `globals.css` and `vitest.config.ts`. No data or migration to unwind.

## 10. Responsive verification + known limitations
- **Responsive check (live `/ui` via `next dev`):**
  - **Desktop (~1440+):** clean — all 16 swatches in an auto-fill grid, type specimens
    and radii render faithfully to Prototype 9; no overlap/clipping.
  - **768px (tablet):** clean reflow — swatch grid drops to 4 columns, header/lede wrap
    cleanly; no overlap/clipping.
  - **360px (mobile):** capture was environmentally unreliable in this session (CDP
    `Page.captureScreenshot` timeout, then a doubled-frame artifact) — a **capture
    glitch, not a layout defect.** The layout is responsive-by-construction (auto-fill
    `minmax(120px,1fr)` grid + `flex-wrap` + relative units); reviewer can confirm at
    the live URL in DevTools device mode. **Binary PNGs not committed to the repo.**
- Webfonts declared as family stacks; actual `next/font` loading deferred (noted in spec).
- Legacy primitives (Button/Badge/…) still on Tailwind default colours until D0.4.
- Visual parity with Prototype 9 asserted by build + live render, not pixel diff.
- Webfonts declared as family stacks; actual `next/font` loading deferred (noted in spec).
- Legacy primitives (Button/Badge/…) still on Tailwind default colours until D0.4.
- Visual parity with Prototype 9 asserted by build + swatch render, not pixel diff.

## 11. Working-tree hygiene
Confirmed: this bundle's file set is disjoint from BE's committed F0.2 and from the
uncommitted v2 spec edits. No unrelated changes bundled into D0.1.
