[build]
builder = "nixpacks"

[build.nixpacks]
installCmd = "pip install -e '.[dev]'"

[deploy]
startCommand = "uvicorn src.api.server:app --host 0.0.0.0 --port ${PORT:-8098}"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
