# Session memory (session 32599d8e, 791 turns)

Deterministic ledger recovered from the session transcript. Full detail: `ctxpack hydrate` on the session .ctx, or grep the raw transcript.

_Decision lint armed: 768 comparisons over 17 decisions — 768 constraint pairs (167 turn-gated), 0 protected-subject checks; 0 unresolved._

## Constraints (verbatim — do not violate)
- Do not implement OKF, Ossie, DataBindingProfile or Snowflake integration during P3. (turn 1)
- Signoile must be logical-complete and binding-ready only. (turn 1)
- Do not self-grant a gate. (turn 1)
- Constraint: canonical `main` stays clean at `0b0245c`; P3.0 sits on `build/P3.0` @ `fc261e0` as an untrusted diff, HELD — I do not self-grant the gate; on your grant I start P3.1 (keyless `AuthorityHostClient` + wire protocol + transports + protected config + `E_AUTHORITY_UNAVAILABLE` + interruption→recovery), TDD red→green. (turn 157)
- Constraint: OKF, Ossie, `DataBindingProfile`, and Snowflake integration are OUT of P3; Signoile is authored logical-complete + binding-ready only. (turn 157)
- Do not amend or erase the immutable review SHA, do not touch main, and do not begin P3.1 concurrently. (turn 158)
- State accurately that AdapterRequest/Response never carry credentials, while authority IPC necessarily carries the exact OperationCredential transiently. (turn 158)
- Do not combine missing source/evidence or missing evaluation/owner. (turn 158)
- Do not leave pinned download versus platform-specific vendored wheels unresolved. (turn 158)
- Define the skill archive format/version/manifest/verifier and use generic deterministic ZIP primitives; do not apply candidate-document validation to a skill bundle. (turn 158)
- Constraint: `fc261e0` remains immutable in history and `main` stays at `0b0245c`; P3.0a (`080ef16`) is HELD as an untrusted diff — I do not self-grant, and P3.1 does not start until the authority grants the corrected freeze. (turn 329)
- Create a small documentation-only P3.0b commit on top; preserve both earlier immutable SHAs and do not start P3.1 yet. (turn 330)
- Freeze one shared readiness gate: resume may report blockers; checkpoint may preserve an incomplete workspace; validate and package must fail while blocking questions remain. (turn 330)
- Constraint: `fc261e0` and `080ef16` remain immutable in history and `main` stays at `0b0245c`; P3.0b (`d2997ad`) is HELD as an untrusted diff — I do not self-grant, and P3.1 does not start until the authority grants the corrected freeze. (turn 451)
- Binding clarification: the shared readiness gate must not make AdapterSession snapshot/resume fail. (turn 455)
- Resume must return blocking questions and checkpoint must preserve incomplete workspaces. (turn 455)
- - Credentials cross only authenticated authority IPC transiently and never enter adapter JSON, argv, environment, logs or files. (turn 455)
- - E_AUTHORITY_UNAVAILABLE must survive every provider-call wrapping point. (turn 455)
- Do not self-grant P3.1. (turn 455)
- Constraint: the P3.1 server pin is a shared symmetric channel secret (mutual HMAC), not asymmetric public-key pinning, and config-writability is enforced on POSIX only — both recorded as honest limitations; asymmetric pinning is future HTTPS work outside P3. (turn 789)
- Constraint: `main` stays at `0b0245c` and the freeze chain `fc261e0→080ef16→d2997ad` is preserved; P3.1 (`15d8a5c`) is HELD as an untrusted diff — I do not self-grant, and P3.2 does not start until you grant P3.1. (turn 789)

## Decisions
- Decision: froze the authority surface to the actual 9-method `AuthoringTrustProvider` and removed r2's `attest_journal`/`verify_journal`/provider-`prepare_intent`, because none exist at `0b0245c` — credential issuance is an out-of-band `AuthoringIntent`→`OperationCredential` exch (turn 157)
- Decision: named the keyless proxy `AuthorityHostClient` and froze `owak checkpoint` to require exactly one new read-only `export_workspace` adapter op converging on `build_workspace_archive`. (turn 157)
- Decision: froze skill bundles as `owak skill build --host {codex|claude}`, Codex-first/Claude-second, with a byte-identical shared core and test-enforced parity. (turn 157)
- Decision: created P3.0a as a new documentation-only commit `080ef16` on top of `fc261e0` (not amended), correcting all nine authority-flagged contradictions grounded in `0b0245c`, keeping `main` clean and not starting P3.1. (turn 329)
- Decision: froze candidate ratification as a host-side human control rather than a kernel proposer≠confirmer invariant, because `_validate_confirmation_actor` (`authoring.py:2145-2148`) enforces only confirmer role membership, not principal distinctness. (turn 329)
- Decision: rewrote F1 as single-defect tests at the real kernel refusal points and recorded new residual R2 — `package` does not hard-gate Definition-of-Ready, so missing dev/regression eval surfaces only as a blocking DoR question, not a package refusal. (turn 329)
- Decision: excluded `advance_authority` from the authority wire allowlist and made the authoring client hard-refuse it, since its only kernel call site is `install_authority` recovery (`authoring.py:3196`), never the authoring loop. (turn 329)
- Decision: resolved the runtime model as two artifacts — an exact-version separately-installed `owak` runtime plus a thin wheels-free/engine-free portable skill bundle — and defined a distinct `ontowiz-skill-bundle` archive format/manifest/verifier that reuses generic ZIP determin (turn 329)
- Decision: created P3.0b as a new documentation-only commit `d2997ad` on top of `080ef16` (no amend; `fc261e0` and `080ef16` both preserved), applying the five final corrections, keeping `main` clean and not starting P3.1. (turn 451)
- Decision: reversed R2 from "future hardening" to an in-scope F1 defect — froze one shared readiness gate (reusing `compile_questions`, no parallel validator) where `validate` and `package` fail while blocking questions remain, `resume` reports, and `checkpoint` is exempt, with ev (turn 451)
- Decision: corrected the F1 refusal mapping so missing `owner_role` fails at `propose` via model validation (`vnext.py:318`/`:826`), and split abstention into two single-defect tests — CandidateArtifact `abstention_conditions` (`vnext.py:319`) vs DecisionContract `materially_unsaf (turn 451)
- Decision: froze the exact confirmation flow (owak submits intent → host human approval in a separate session → intent-bound credential returned to the waiting keyless CLI → owak confirm applies) and the canonical skill suffix `.owskill` with a per-host upload mapping. (turn 451)
- Decision: to avoid an import cycle, the `AuthorityClientError` hierarchy lives in a dependency-free `authority_errors.py` that `authoring.py`, `authority_client.py`, and `adapters.py` all import; the 9 kernel wrapping sites each gain a narrow `except AuthorityClientError: raise`  (turn 490)
- Decision: implemented the transport over `multiprocessing.connection` named-pipe/UDS with an `authkey` because it provides authenticated local IPC with a bidirectional HMAC challenge out of the box; HTTPS is deferred out of P3. (turn 789)
- Decision: placed the `AuthorityClientError` hierarchy in a dependency-free `authority_errors.py` and added narrow pass-throughs at all 9 kernel `except Exception` provider-call sites, so a transport failure survives kernel wrapping and maps to `E_AUTHORITY_UNAVAILABLE`, while a r (turn 789)
- Decision: reused the existing `tests/adapters` `ExternalTestProvider` behind a tests-only reference host to prove the keyless client end-to-end over a real pipe, keeping `src` and the skill bundle free of any host, provider, or keys. (turn 789)

## Exact identifiers (verbatim) (showing 5 highest-rank of 30 — full set in the ledger)
- #3 [pr] (turn 143)
- #1 [pr] (turn 168)
- #5 [pr] (turn 176)
- #2 [pr] (turn 209)
- #4 [pr] (turn 218)