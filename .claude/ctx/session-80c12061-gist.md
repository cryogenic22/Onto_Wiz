# Session memory (session 80c12061, 599 turns)

Deterministic ledger recovered from the session transcript. Full detail: `ctxpack hydrate` on the session .ctx, or grep the raw transcript.

_Decision lint armed: 279 comparisons (23 decisions × 15 banked constraints), 0 unresolved._

## Constraints (verbatim — do not violate)
- Constraint: no volatile value (timestamp, run-id, local path, eval result) is ever written anywhere inside a candidate directory. (turn 56)
- Constraint: implementation (cut `build/S1.1` from baseline `1dc26ca`, red-green) starts only on the delta-review READY — the builder does not self-verify the gate that just caught seven production defects. (turn 87)
- > One binding implementation clarification: pack.yaml must serialize: (turn 88)
- > candidate_digest is excluded only from the material hashed to calculate itself; it must still be stored in (turn 88)
- Continue directly through green implementation; do not stop after test scaffolding. (turn 88)
- Do not expand into S1.2, S1.3, F0.6A, runtime wiring, or unrelated refactoring. (turn 88)
- Do not draft evidence in advance. (turn 88)
- > Do not ask for intermediate permission after the red tests. (turn 88)
- Constraint: S1.1 is submitted as immutable review SHA `9d52b47`; I am not marking it VERIFIED — INT alone does that after implementation review. (turn 325)
- Constraint: I did not act on finding #3 — it arrived truncated/garbled; I'm requesting a clean re-send rather than implementing a guess, and awaiting your steer on whether to reseal the pre-existing stale fixture. (turn 380)

## Decisions
- Decision: adopt a spec-red gate — before I submit any mini-spec, enumerate its promised invariants and write the falsification cases (determinism, completeness/closure, injectivity, state-composition, trust-boundary) into the spec, and pin the exact serialization envelope, so the (turn 3)
- Decision: `candidate_digest` hashes an explicit field allowlist and reads each payload file's digest out of `output_inventory` (never re-hashing files or listing `pack.yaml`/`pack.sig`), making the content id non-self-referential and the `pack.sig` seal cycle-free. (turn 56)
- Decision: dropped `compiled_at` from the candidate so repeated builds are byte-identical; build time is provenance of the registration/release event, recorded outside the candidate by S1.3/F0.6A. (turn 56)
- Decision: `write_pack` never migrates v1→v2 in place — a v1 target at the same version raises `CandidateDigestConflictError` and migration requires a version bump; authorized in-place migrate is deferred to F0.6A/Step 10. (turn 56)
- Decision: amend-1 — `PackManifest.manifest_version` now defaults to `1`, so a manifest with no version field loads as v1 (compat), matching the §4 compatibility rule; `compile_pack` sets `2` explicitly. (turn 87)
- Decision: amend-2 — the candidate `pack.yaml` is serialized from an explicit allowlist = {digest `core`} ∪ {`artifact_count`, `artifact_kinds`}. (turn 87)
- Decision: resolved the amend-2 `evals` contradiction via your Option 1 — `write_pack` strips mutable/clock fields from the candidate; `signed` is kept as a fixed-deterministic serialized field (satisfying amend-2's own rule) so runtime `signed is True` stays green; the catalog ev (turn 325)
- Decision: split the persistence half into `writer.py` (re-exported from `compiler`) to satisfy the CK quality gate (file-size/function-size/complexity) without changing the public import surface. (turn 325)
- Decision: did not modify `packs/commercial_analytics/0.1.0`; its failing test is pre-existing at baseline `1dc26ca` (proven) and out of S1.1 scope — surfaced for INT rather than absorbed. (turn 325)
- Decision: closed finding #1 by validating declared inventory paths against an exact grammar before any filesystem access and rejecting all symlink/reparse entries with realpath containment, because `verify_candidate_dir` is the S1.2 load-time verifier for untrusted packs. (turn 380)
- Decision: closed finding #2 by re-verifying the existing target on an idempotent hit and raising the new typed `CorruptCandidateError` on a same-digest-but-corrupt target. (turn 380)
- Decision: I framed the report around the platform's own status vocabulary (BUILT/SPECIFIED/ROADMAP) and kept the honest self-assessment — consume side real, produce side thin — rather than presenting the roadmap as built, because overstatement is the exact failure mode this codeb (turn 434)
- Decision: consolidate the five 2026-07 architecture reviews into one PROPOSED plan that maps the Enterprise-Maturity epics onto existing backlog cards rather than minting a parallel Loop 0–8/gate namespace, per the standing "no renumbering, no parallel paths" rule. (turn 488)
- Decision: the `test_write_results_reseals_signed_pack` failure is an EOL-normalization artifact (CRLF main tree passes, LF worktree fails via raw-byte `pack.sig`), not a stale fixture — the fix is EOL-robust seal computation and `PROJECT_STATUS.md:310` is stale. (turn 488)
- Decision: recommend treating review 6 as gating — reframe the analytics vertical as a pre-registered, kill-gated competitive experiment against a warehouse-native baseline and park the platform's breadth (additional domains, multi-pack composition, Forge reputation, property grap (turn 491)
- Decision: ratified direction encoded — Onto_Wiz moves to hypothesis-validation mode; only the minimal integrity+evidence+decision-eval spine (finish S1.1/F0.10, EOL-robust seal, fixture flags, minimal source/evidence/applicability/decision-eval contracts, the two eval datasets, m (turn 502)
- Decision: proposed the validation-mode work split — BE owns the minimum spine + three-arm harness + decision-eval runner + execution receipts + invalidation-on-serve (all gated by the A1 review of S1.1/F0.10); Core owns the one shared minimal kernel (rights/applicability/evidence (turn 505)
- Decision: applied the pre-registration review to the plan — consolidation doc is now Revision 3 (H1/H2a/H2b split, spine time/scope ceiling, evaluation firebreak, corrected shared-kernel failure condition = duplicated control spine not a second ontology, single locked baseline, m (turn 543)
- Decision: endorsed the "Codex/Claude authors, OntoWiz governs-and-scales" model and specified the OntoWiz Authoring Kit as a framework-neutral Codex skill backed by a small repo that **vendors the real `ontowiz-spec` contracts** (not a fork), with fail-closed validators, candidat (turn 550)
- Decision: consolidated the authoring-kit discussion into a v0 build spec (Rev 2) that adopts scaffolding-by-archetype, candidate-only source ingestion, a generated-not-live visual explorer, a four-loop Grill/Forge authoring engine with a Question Compiler + Domain Decision Record (turn 557)
- Decision: agree with the independent-Authoring-Kit / platform-importer / `.owpack`-contract architecture as the *target*, with two caveats — define the `.owpack` manifest contract now but stage the full importer/compat-CI/separate-release until the hypothesis passes, and recogniz (turn 560)
- Decision: executed phase one of the independent Authoring Kit — created `OntoWiz_Authoring_Kit_Claude` (git-init, `main`, Onto_Wiz untouched) and produced the implementation plan, ownership map, contract decisions, and acceptance gates before any code, with six gated adversariall (turn 583)
- Decision: banked the session (ledger + memory) for a clean resume — added a dedicated `ontowiz-authoring-kit-build` memory recording that phase-1 planning is complete in `OntoWiz_Authoring_Kit_Claude` (uncommitted, Onto_Wiz read-only) and that the build is gated on owner approval (turn 598)

## Exact identifiers (verbatim) (showing 9 highest-rank of 46 — full set in the ledger)
- #3 [pr] (turn 3)
- #5 [pr] (turn 3)
- f8a79a3 [git_sha] (turn 87)
- 0.1.0 [version] (turn 199)
- 9d52b47 [git_sha] (turn 308)
- 4eead82 [git_sha] (turn 376)
- 5ebbda3 [git_sha] (turn 466)
- 0.3.0 [version] (turn 466)
- fa070fa [git_sha] (turn 598)