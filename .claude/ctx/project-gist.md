# Project memory (6 earlier sessions, oldest first)

Cross-session ledger rollup. Detail per session: `ctxpack session decisions --session <id>`.

## Constraints (verbatim — do not violate)
- > One binding implementation clarification: pack.yaml must serialize: (s:80c12061#turn88)
- > candidate_digest is excluded only from the material hashed to calculate itself; it must still be stored in (s:80c12061#turn88)
- Continue directly through green implementation; do not stop after test scaffolding. (s:80c12061#turn88)
- Do not expand into S1.2, S1.3, F0.6A, runtime wiring, or unrelated refactoring. (s:80c12061#turn88)
- Do not draft evidence in advance. (s:80c12061#turn88)
- > Do not ask for intermediate permission after the red tests. (s:80c12061#turn88)
- Constraint: S1.1 is submitted as immutable review SHA `9d52b47`; I am not marking it VERIFIED — INT alone does that after implementation review. (s:80c12061#turn325)
- Constraint: I did not act on finding #3 — it arrived truncated/garbled; I'm requesting a clean re-send rather than implementing a guess, and awaiting your steer on whether to reseal the pre-existing stale fixture. (s:80c12061#turn380)
- Constraint: consolidation (multi-SME dedup, contradiction detection, merge policy) has no owner, design, or milestone anywhere in the current program — the report schedules a minimal curation-time pass at M3 and cross-pack conflict surfacing with the M4 resolver; until then external narratives should say "governed authoring," not "consolidation." (s:fde086af#turn104)
- Constraint: A1 (INT review of S1.1 at `4eead82`) gates every build loop — nothing in Waves B–G should start before it closes. (s:fde086af#turn154)
- Constraint: before minting any new unit ID, reconcile against DELIVERY_LOOPS_BACKLOG_2026-07.md plus the B0–B12/GM0–GM7/Slice A–J inventories — the crosswalk alone is not the full card set. (s:fde086af#turn177)
- Constraint: no BE unit can reach the completion gate until the stale `pack.sig` fixture blocker is dispositioned — treat `verify-audit` gate 1 as unreachable, not as a per-unit caveat. (s:fde086af#turn508)
- Constraint: I did not run verify-audit for the verdict — the builder never self-verifies the A1 gate; all evidence in the packet is builder-reported and labeled for INT's independent confirmation. (s:01df9319#turn37)
- Constraint: I remain HELD behind A1 — no Wave B–G build loop starts until INT closes the S1.1 review at `4eead82`. (s:01df9319#turn37)
- Constraint: the Authoring Kit is a separate repo and is not gated by A1 — work there breaks no Onto_Wiz hold; the A1 hold still blocks only Onto_Wiz Waves B–G. (s:01df9319#turn64)
- Constraint: the `.skill` distributable inherits the `.owpack` exclusion contract — no held-out answers, no eval vault, no protected scorer/receipt providers, and no raw sources beyond the transfer contract — so packaging never becomes a leak path for protected material. (s:01df9319#turn67)
- Constraint: I'm keeping the two areas I verified myself (the §C8 fail-closed test and the skill/adapter packaging surface) out of the background harvest's scope to avoid duplicated review of the same files. (s:01df9319#turn96)
- Constraint: canonical repo stays clean at `0b0245c`; I write no code until each P3 gate is granted; F1 runs before the Signoile demo; Signoile stays fictional/non-clinical with synthetic evidence only. (s:01df9319#turn167)
- Constraint: OKF export (I1) is buildable on current contracts, but Ossie export (I2) is not — verified via `vnext.py`: the metric contract carries a human-readable `formula` + `formula_inputs` and provenance bindings only, with no dataset FQN, SQL expression, dialect, or relationship model, so under the spec's fail-closed rule every metric returns `blocked_missing_binding`; extending the authoring contract to capture executable analytics bindings is a prerequisite unit before I2. (s:01df9319#turn183)
- Constraint: Snowflake conformance/deployment (interop Slice I4) is platform-repo work, not the Authoring Kit — I will not build it in this repo. (s:01df9319#turn183)
- Constraint: I remain HELD on P3 — no code until you grant the gates and answer the client↔host transport and client-authentication questions. (s:01df9319#turn183)
- Constraint: the Signoile demo's `confirm` runs as a separate confirmer session unavailable to the drafting agent — proposer and confirmer are genuinely distinct principals, so no single agent drives authoring end-to-end. (s:01df9319#turn190)
- Constraint: still no code — canonical repo clean at `0b0245c`; I begin P3.1 only on your go on r2, and each P3 step is submitted as an independently reviewed untrusted diff. (s:01df9319#turn190)
- The important correction is this: **we should not simply add Snowflake FQNs and SQL expressions directly to the reusable domain pack.** Those are usually tenant-, environment- and platform-specific. (s:01df9319#turn191)
- That profile could live in the local `.owworkspace` or platform custody; it should not automatically enter the distributable generic `.owpack`. (s:01df9319#turn191)
- Constraint: P3.1 is still held pending your go on `P3_REVISED_MINISPEC.md` r2; no code until then, and each P3 step ships as an independently reviewed untrusted diff. (s:01df9319#turn201)
- Constraint: on resume, read `project_ontowiz_authoring_kit.md` (auto-loaded) + `OntoWiz_Authoring_Kit_Claude/design/P3_REVISED_MINISPEC.md` r2 before acting; do not touch the canonical repo at `0b0245c` and do not self-grant gates until the authority's GO on r2. (s:01df9319#turn227)