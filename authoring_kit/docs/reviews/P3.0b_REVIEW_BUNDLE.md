# P3.0b — Review Bundle (final P3.0 correction pass · untrusted · HELD)

**Unit:** P3.0b — final correction pass on the P3.0 contract freeze (documentation-only) · **Branch:**
`build/P3.0`, a **new commit on top of `080ef16`** (no amend; `fc261e0` **and** `080ef16` remain in
history) · **Baseline:** canonical `main` clean at `0b0245c` (untouched) · **Gate:** authority review —
**not self-granted.** P3.1 **not** started.

> The authority held `080ef16` and returned five corrections, noting this should be the **final** P3.0
> correction pass. Corrected in place; all evidence is **builder-reported for independent confirmation.**

---

## 1. The five corrections (full grounding in `docs/contracts/P3.0a_CORRECTIONS.md` → "P3.0b")

| # | Correction | Where |
|---|---|---|
| b1 | **R2 is in-scope, not future** — freeze **one shared readiness gate**: `resume` reports blockers, `checkpoint` may preserve an incomplete workspace, **`validate` + `package` must fail while blocking questions remain**, every `DecisionContract` needs linked **dev + regression** eval; F1 closes it before Signoile (reuses `compile_questions`, no parallel validator) | `P3.0_CONTRACT_FREEZE.md §6/§6a/§8/§9`, `CONTRACT_CLI.md §3/§3a` |
| b2 | **Exact confirmation flow** — `owak` submits the intent → host approves via a **separate human UI/session** → host returns the **intent-bound credential** to the **waiting keyless CLI** → `owak confirm` applies it | `CONTRACT_AUTHORITY.md §5`, `CONTRACT_CLI.md §5` |
| b3 | **F1 mappings corrected** — missing `owner_role` fails at **`propose`** (`vnext.py:318/826`); use `source_document_ids`/`evidence_refs`; **CandidateArtifact `abstention_conditions`** and **DecisionContract `materially_unsafe_answers`** are separate tests; one defect per case | `P3.0_CONTRACT_FREEZE.md §6` |
| b4 | **Decision log scrubbed** of stale in-P3 HTTPS (D1: local IPC only) and "pinned requirements *or* vendored wheels" (D8: two-artifact runtime) | `P3.0_CONTRACT_FREEZE.md §3` |
| b5 | **Exact canonical suffix `.owskill`** (no "e.g.") + per-host **upload mapping** (Codex skill dir; Claude Desktop skill) | `CONTRACT_SKILL_BUNDLE.md §1.1` |

## 2. Files

Modified: `CONTRACT_AUTHORITY.md`, `CONTRACT_CLI.md`, `CONTRACT_SKILL_BUNDLE.md`, `P3.0_CONTRACT_FREEZE.md`,
and `P3.0a_CORRECTIONS.md` (now the running correction ledger, P3.0a + P3.0b). Added: this bundle. No
tracked code file changed; `main` stays `0b0245c`; `fc261e0` and `080ef16` unchanged in history.

## 3. Evidence (builder-run — authority to confirm; nothing self-granted)

| Gate | Command | Result |
|---|---|---|
| Doc-only | `git status` / `git diff --stat HEAD` | 5 `.md` modified + 1 new `.md`; **0 tracked code files changed** |
| Tests | `python -m pytest -q` | **exit 0; 263 items, all green** (0 failed / 0 error / 0 skipped) |
| Lint | `python -m ruff check .` | **All checks passed!** |
| Types | `python -m mypy` (strict) | **Success: no issues found in 20 source files** |
| Vendor lock | `python tools/verify_vendor_lock.py` | **vendor-lock-ok** (exit 0) |
| Source-origin lock | `python tools/verify_source_lock.py` | **source-lock-ok** (exit 0) — **machine-local only** (R1: hardcoded path `locks/source-origin.json:3`; not CI-enforced) |
| Coverage | `fail_under = 80.77` | unaffected by a doc-only change |

## 4. Not done / boundaries (honest)

- **No code.** P3.1–P3.4 not started; `main` stays `0b0245c`; neither `fc261e0` nor `080ef16` amended.
- **R2 is now CLOSED in P3** (b1) via the shared readiness gate — no longer a residual/future item.
- **R1** source-origin lock stays machine-local / not CI-enforced (green here only because the sister repo
  sits at the hardcoded path); begin closing it in the P3.3 source-lock check.
- All §3 gates were builder-run; the authority should re-run to confirm.

## 5. How to review

1. `git -C OntoWiz_Authoring_Kit switch build/P3.0 && git diff 080ef16 --stat` → Markdown-only.
2. Read `docs/contracts/P3.0a_CORRECTIONS.md` (the "P3.0b" section at top), then spot-check `file:line`:
   `authoring.py:2137-2148` (b2 basis), `vnext.py:318/319/824/826` (b3), `archive.py:1479` (b5).
3. Confirm `P3.0_CONTRACT_FREEZE.md §6/§6a` reads single-defect + one shared readiness gate, and §3 is free
   of in-P3 HTTPS / wheels phrasing.
4. Re-run the §3 gates independently.

## 6. Requested gate decision

Grant **P3.0 (as corrected through P3.0b)** → builder proceeds to **P3.1** (keyless `AuthorityHostClient`
over authenticated local IPC + wire protocol + protected config + `E_AUTHORITY_UNAVAILABLE` survival +
interruption→recovery), TDD red→green, as the next untrusted diff. **HELD until then. P3.1 not started
concurrently.**
