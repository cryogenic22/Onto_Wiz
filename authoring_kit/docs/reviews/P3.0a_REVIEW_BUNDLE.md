# P3.0a — Review Bundle (corrected resubmission · untrusted · HELD)

**Unit:** P3.0a — correction pass on the P3.0 contract freeze (documentation-only) · **Branch:**
`build/P3.0`, a **new commit on top of `fc261e0`** (P3.0 **not** amended; `fc261e0` remains in history) ·
**Baseline:** canonical `main` clean at `0b0245c` (untouched) · **Submitted by:** builder · **Gate:**
authority review — **not self-granted.**

> The authority withheld the P3.0 gate at `fc261e0` for correctness contradictions. This resubmission
> corrects the frozen contracts in place and records the nine corrections. All evidence is
> **builder-reported for the authority's independent confirmation.**

---

## 1. What changed (nine corrections — full grounding in `docs/contracts/P3.0a_CORRECTIONS.md`)

| # | Correction | Where |
|---|---|---|
| 1 | Kernel does **not** enforce proposer ≠ confirmer (`_validate_confirmation_actor` checks **role membership** only, `authoring.py:2145-2148`); frozen as **host-side human ratification**; platform approval stays out of P3; `owak confirm` preserved as execution path | `CONTRACT_AUTHORITY.md §5` |
| 2 | `AdapterRequest/Response` never carry a credential; the **authority IPC necessarily carries the exact `OperationCredential` transiently** | `CONTRACT_AUTHORITY.md §2`, `CONTRACT_IPC_WIRE.md §1/§4`, `CONTRACT_CLI.md §5` |
| 3 | `advance_authority` **hard-refuses** on the authoring client and is **off the wire allowlist** (only call site is `install_authority` recovery, `authoring.py:3181/3196`); admin is a separate channel | `CONTRACT_AUTHORITY.md §1/§2`, `CONTRACT_IPC_WIRE.md §1`, `CONTRACT_CLI.md §4` |
| 4 | Protected endpoint/pin config is **read-only to the drafting principal** (admin-writable only) | `CONTRACT_IPC_WIRE.md §3` |
| 5 | F1 rewritten as **single-defect** tests at **real** refusal points; source/evidence split; eval/owner split; missing-eval is a **blocking DoR question**, not a package refusal (new gap R2) | `P3.0_CONTRACT_FREEZE.md §6/§9` |
| 6 | **Two-artifact runtime** frozen: exact-version installed `owak` runtime (A) + **thin** portable bundle (B, no wheels/engine); ambiguity resolved | `CONTRACT_RUNTIME.md §1-3`, `CONTRACT_SKILL_BUNDLE.md §3` |
| 7 | `AuthorityTransportError` **survival** defined: distinct type, passes kernel wrap sites, first-matched `except` arm → `E_AUTHORITY_UNAVAILABLE` on every verb | `CONTRACT_IPC_WIRE.md §5`, `CONTRACT_CLI.md §6` |
| 8 | HTTPS production transport marked **future / out of P3**; P3.1 = authenticated local IPC only | `CONTRACT_AUTHORITY.md §1/§5`, `CONTRACT_IPC_WIRE.md §2` |
| 9 | Skill archive **format/version/manifest/verifier** defined (`ontowiz-skill-bundle`) from **generic** ZIP primitives; **no candidate-document validation** on a skill bundle | `CONTRACT_SKILL_BUNDLE.md §1/§1.1` |

## 2. Files

Modified (corrected in place): the six `docs/contracts/*` freeze files. Added: `docs/contracts/`
`P3.0a_CORRECTIONS.md` (delta record) + this bundle. No file tracked at `0b0245c` was modified; `main`
stays at `0b0245c`; `fc261e0` is unchanged in history.

## 3. Evidence (builder-run — authority to confirm; nothing self-granted)

| Gate | Command | Result |
|---|---|---|
| Doc-only | `git status` / `git diff --stat HEAD` | 6 contract `.md` modified + 2 new `.md`; **0 tracked code files changed** |
| Tests | `python -m pytest -q` | **exit 0; 263 items, all green** (0 failed / 0 error / 0 skipped) |
| Lint | `python -m ruff check .` | **All checks passed!** |
| Types | `python -m mypy` (strict) | **Success: no issues found in 20 source files** |
| Vendor lock | `python tools/verify_vendor_lock.py` | **vendor-lock-ok** (exit 0) |
| **Source-origin lock** | `python tools/verify_source_lock.py` | **source-lock-ok** (exit 0) — see caveat |
| Coverage | `fail_under = 80.77` (`pyproject.toml:50`) | unaffected by a doc-only change; ratchet intact |

**Source-lock caveat (honest).** `verify_source_lock.py` returns `source-lock-ok` **on this machine only**
because the sister repo happens to sit at the hardcoded `locks/source-origin.json:3` path
`C:/Users/kapil/Documents/Onto_Wiz`. This is exactly residual **R1** (`P3.0_CONTRACT_FREEZE.md §9`): the
check is **not CI-enforced** and is **un-runnable off the author's machine**. A green result here is *not*
evidence the upstream anti-drift guarantee is enforced.

## 4. Not done / boundaries (honest)

- **No code.** P3.1–P3.4 not started; `main` stays at `0b0245c`; P3.0/`fc261e0` not amended.
- **New residual R2** (surfaced by correction #5): `package` does not hard-gate Definition-of-Ready, so a
  missing dev/regression eval or an undeclared owner role is a **blocking DoR question**, not a hard
  package refusal. F1 asserts that real behaviour; a hard DoR package-gate is a candidate future hardening
  unit, **not** invented in P3.
- **R1** source-origin lock stays machine-local / not CI-enforced (§3 caveat).
- All §3 gates were builder-run; the authority should re-run to confirm.

## 5. How to review

1. `git -C OntoWiz_Authoring_Kit switch build/P3.0 && git diff fc261e0 --stat` → Markdown-only.
2. Read `docs/contracts/P3.0a_CORRECTIONS.md`, then spot-check each correction's `file:line` against the
   `0b0245c` tree (esp. #1 `authoring.py:2145-2148`, #3 `:3181/3196`, #5 `:1854-1889`, #9 `archive.py:61-63`).
3. Confirm the six contract files now read consistently with the corrections.
4. Re-run the §3 gates independently.

## 6. Requested gate decision

Grant **P3.0 (as corrected by P3.0a)** → builder proceeds to **P3.1** (keyless `AuthorityHostClient` over
authenticated local IPC + wire protocol + protected config + `E_AUTHORITY_UNAVAILABLE` survival +
interruption→recovery), TDD red→green, as the next untrusted diff. **HELD until then. P3.1 is not started
concurrently.**
